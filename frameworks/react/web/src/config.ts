export const SHARED_WORKER_URL = `${window.location.origin}/assets/shared-worker.js`;
export const BROKER_URL = `${window.location.origin}/iframe-broker.html`;
export const LAYOUT_URL = `${window.location.origin}/default.layout.fin.json`;
